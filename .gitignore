# Xcode
xcuserdata/
DerivedData/
*.xcuserstate

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store

# Secrets — never commit API keys (FAA NOTAM, Leidos)
.env
.env.*
!Server/.env.example
# FAA NMS onboarding pack: the spreadsheet in it is the client id/secret.
onboardingontonmsapiinc0028461/

# Server deployment scratch (ingest intermediates, cron logs)
Server/ingest-work/
Server/logs/

# Generated download artifacts (multi-GB MBTiles/zips) — keep the dir, not the contents
Server/Public/artifacts/*
!Server/Public/artifacts/.gitkeep
